# 🧪 Test Environment Configuration
# NODE_ENV=test

# 🌍 Application
NODE_ENV=test
PORT=3001
HOST=127.0.0.1

# 🗄️ Database PostgreSQL (Test)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=test_user
DATABASE_PASSWORD=test_password
DATABASE_NAME=cleanarchi_test
DATABASE_SSL=false
DATABASE_POOL_SIZE=5

# 🍃 MongoDB (Test)
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_USERNAME=test_user
MONGODB_PASSWORD=test_password
MONGODB_DATABASE=cleanarchi_test

# ⚡ Redis (Test)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=test_password

# 🔐 JWT Configuration (Test)
ACCESS_TOKEN_SECRET=test-access-token-secret-for-testing-only
REFRESH_TOKEN_SECRET=test-refresh-token-secret-for-testing-only
ACCESS_TOKEN_EXPIRATION=300
REFRESH_TOKEN_EXPIRATION_DAYS=1

# 🔒 Security Settings (Fast for tests)
BCRYPT_ROUNDS=1

# 👤 User Sessions (Short for tests)
USER_SESSION_DURATION_MINUTES=5
USER_CACHE_RETENTION_MINUTES=5

# 📧 Email Configuration (Mock)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
EMAIL_FROM="Test App <test@test.local>"

# 🌐 Application
APP_NAME="Clean Architecture NestJS (Test)"
APP_VERSION="1.0.0-test"
APP_URL="http://localhost:3001"

# 🛡️ Security (Disabled for tests)
ENABLE_CORS=true
CORS_ORIGIN=*
ENABLE_RATE_LIMITING=false
ENABLE_COMPRESSION=false

# 📊 Monitoring (Disabled for tests)
ENABLE_SWAGGER=false
ENABLE_METRICS=false
ENABLE_PROFILING=false

# 🔧 Features (All enabled for testing)
FEATURE_ENABLE_REGISTRATION=true
FEATURE_ENABLE_PASSWORD_RESET=true
FEATURE_ENABLE_EMAIL_VERIFICATION=true
FEATURE_ENABLE_TWO_FACTOR_AUTH=true

# 📝 Logging (Minimal for tests)
LOG_LEVEL=error
LOG_FORMAT=simple
LOG_COLORIZE=false
LOG_TIMESTAMP=false
