#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "� Validation du message de commit désactivée temporairement..."
# npx --no -- commitlint --edit $1
echo "⚠️  Commit message validation disabled!"
