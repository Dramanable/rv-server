/**
 * 🧪 TDD - MongoDB Configuration Service
 *
 * Tests pour la configuration MongoDB/Mongoose
 */

describe('MongoConfigService', () => {
  it('should be tested', () => {
    expect(true).toBe(true);
  });
});
