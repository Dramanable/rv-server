# � Configuration Développement Docker - NestJS Clean Architecture
# Exemple de fichier .env pour développement avec Docker Compose

# 🌍 Application
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# 🗄️ Database PostgreSQL (Docker Service)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cleanarchi_dev
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=cleanarchi_dev

# 🍃 MongoDB (Docker Service)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=admin
MONGO_DB=cleanarchi_dev
MONGODB_HOST=mongodb
MONGODB_PORT=27017
MONGODB_USERNAME=admin
MONGODB_PASSWORD=admin
MONGODB_DATABASE=cleanarchi_dev

# ⚡ Redis (Docker Service)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis123

# 🔐 JWT Configuration - DEVELOPMENT ONLY
ACCESS_TOKEN_SECRET=dev-access-token-secret-for-development-only-32chars
REFRESH_TOKEN_SECRET=dev-refresh-token-secret-for-development-only-32chars
ACCESS_TOKEN_EXPIRATION=3600
REFRESH_TOKEN_EXPIRATION_DAYS=30

# 🔒 Security Settings - Development Mode
BCRYPT_ROUNDS=4

# 👤 User Sessions
USER_SESSION_DURATION_MINUTES=30
USER_CACHE_RETENTION_MINUTES=60

# 🖥️ pgAdmin (Optional Web Interface)
PGADMIN_EMAIL=admin@cleanarchi.dev
PGADMIN_PASSWORD=admin123

# 🐳 Docker Development Optimization
NODE_OPTIONS=--max-old-space-size=384
NPM_CONFIG_LOGLEVEL=warn
GENERATE_SOURCEMAP=false
NEST_DEBUG=false
NEST_CLI_EXPERIMENTAL_FORK_TYPE_CHECKER=false

# 📧 Email Configuration - Development (Optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_AUTH_USER=your-dev-email@gmail.com
EMAIL_AUTH_PASS=your-app-password
EMAIL_FROM_NAME=CleanArchi Dev
EMAIL_FROM_ADDRESS=your-dev-email@gmail.com

# ===== COMMANDES DOCKER UTILES =====
# Démarrer tous les services : docker-compose up -d
# Arrêter tous les services : docker-compose down
# Voir les logs de l'API : docker-compose logs app
# Rebuilder l'image : docker-compose up -d --build
# Accéder au container : docker-compose exec app sh

# ===== MIGRATION ET BASE DE DONNÉES =====
# Créer une migration : npm run migration:create -- --name=CreateUser
# Lancer les migrations : npm run migrate:run
# Annuler une migration : npm run migrate:revert
# Statut des migrations : npm run migrate:status

# ===== ACCÈS AUX SERVICES =====
# Application NestJS : http://localhost:3000
# pgAdmin (Interface PostgreSQL) : http://localhost:5050
# Swagger Documentation : http://localhost:3000/api
# Health Check : http://localhost:3000/health

# ===== NOTES DE SÉCURITÉ - DÉVELOPPEMENT =====
# ⚠️ Ce fichier contient des valeurs par défaut pour le développement
# ⚠️ NE PAS utiliser ces valeurs en production
# ⚠️ Les secrets JWT sont volontairement faibles pour le développement
# ⚠️ BCRYPT_ROUNDS=4 pour accélérer les tests (utiliser 12+ en production)
