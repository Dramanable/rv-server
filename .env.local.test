# 🔧 Variables d'environnement pour tests d'intégration locaux sans Docker
# Ces variables sont utilisées quand les tests tournent directement sur la machine locale

# 🖥️ Local Services (utilise localhost)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/cleanarchi_test

MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_URL=mongodb://admin:admin@localhost:27017/cleanarchi_test

# 🧪 Test Environment
NODE_ENV=test
CI=false

# 🔐 JWT Secrets for Tests
JWT_SECRET=test_jwt_secret_for_local
JWT_REFRESH_SECRET=test_refresh_secret_for_local
